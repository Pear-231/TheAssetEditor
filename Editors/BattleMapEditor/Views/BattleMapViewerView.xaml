<UserControl x:Class="Editors.BattleMapEditor.Views.BattleMapViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Label Content="Tile Folder:" VerticalAlignment="Center"/>
            <TextBox Width="450"
                     VerticalAlignment="Center"
                     Text="{Binding MapFolderPath, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="e.g. terrain/tiles/battle/domination/waka_chateau"/>
            <Button Content="Load"
                    Command="{Binding LoadCommand}"
                    Margin="4,0,0,0"
                    Padding="8,2"/>
            <Separator Margin="8,0"/>
            <CheckBox Content="Instanced rendering"
                      IsChecked="{Binding UseInstancing}"
                      VerticalAlignment="Center"/>
        </ToolBar>

        <!-- Main content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="120"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="260" MinWidth="140"/>
            </Grid.ColumnDefinitions>

            <!-- Layers panel -->
            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top"
                           Text="Layers"
                           FontWeight="SemiBold"
                           Margin="4,4,4,2"/>
                <ListBox ItemsSource="{Binding Layers}" BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="2"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsVisible}" Content="{Binding Name}" Padding="4,0"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="LightGray"/>

            <!-- 3D Scene -->
            <ContentControl Grid.Column="2" Content="{Binding Scene, Mode=OneTime}"/>

            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="LightGray"/>

            <!-- Resources panel -->
            <DockPanel Grid.Column="4">
                <TextBlock DockPanel.Dock="Top"
                           Text="Resources"
                           FontWeight="SemiBold"
                           Margin="4,4,4,2"/>
                <ItemsControl ItemsSource="{Binding Resources}" FontSize="11">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="36"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding FileName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding PackPath}" Foreground="White"/>
                                <TextBlock Grid.Column="1" Text="{Binding Type}" Foreground="Gray" Margin="4,0"/>
                                <TextBlock Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="✗"/>
                                            <Setter Property="Foreground" Value="Red"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFound}" Value="True">
                                                    <Setter Property="Text" Value="✓"/>
                                                    <Setter Property="Foreground" Value="LimeGreen"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Grid>

        <!-- Status bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
